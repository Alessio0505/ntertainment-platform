const {app}=require('@azure/functions');const {getPool,sql}=require('../shared/db');const {corsHeaders,requireTntUser}=require('../shared/auth');
app.http('TNTTeamStats',{methods:['GET','OPTIONS'],authLevel:'anonymous',handler:async(req,ctx)=>{if(req.method==='OPTIONS')return{status:204,headers:corsHeaders()};try{const user=requireTntUser(req,['Admin','TNTEvaluator']),teamId=+req.query.get('teamId'),schoolYear=String(req.query.get('schoolYear')||''),pool=await getPool();if(!teamId||!schoolYear)return{status:400,headers:corsHeaders(),jsonBody:{error:'Team en dansjaar zijn verplicht.'}};if(user.role!=='Admin'){const a=await pool.request().input('UserId',sql.Int,user.userId).input('TeamId',sql.Int,teamId).query(`SELECT 1 ok FROM dbo.TNTUserTeams WHERE UserId=@UserId AND TeamId=@TeamId`);if(!a.recordset.length)throw Object.assign(new Error('Geen toegang tot dit team.'),{status:403})}const team=(await pool.request().input('TeamId',sql.Int,teamId).query(`SELECT TeamId,Name FROM dbo.TNTTeams WHERE TeamId=@TeamId AND IsActive=1`)).recordset[0];if(!team)throw Object.assign(new Error('Team niet gevonden.'),{status:404});const periods=(await pool.request().input('SchoolYear',sql.NVarChar(20),schoolYear).query(`SELECT PeriodId,Name,SortOrder FROM dbo.TNTEvaluationPeriods WHERE SchoolYear=@SchoolYear AND IsActive=1 ORDER BY SortOrder`)).recordset;const params=(await pool.request().input('TeamId',sql.Int,teamId).input('SchoolYear',sql.NVarChar(20),schoolYear).query(`WITH PD AS(SELECT e.DancerId,e.PeriodId,s.CriterionId,AVG(CAST(s.Score AS decimal(6,3))) Score FROM dbo.TNTEvaluations e JOIN dbo.TNTEvaluationScores s ON s.EvaluationId=e.EvaluationId JOIN dbo.TNTDancers d ON d.DancerId=e.DancerId JOIN dbo.TNTEvaluationPeriods p ON p.PeriodId=e.PeriodId WHERE d.TeamId=@TeamId AND p.SchoolYear=@SchoolYear GROUP BY e.DancerId,e.PeriodId,s.CriterionId) SELECT c.CriterionId,c.Name,c.SortOrder,COALESCE(c.Category,CASE WHEN c.SortOrder<=6 THEN 'Individual' ELSE 'Team' END) Category,p.PeriodId,p.Name PeriodName,p.SortOrder PeriodSort,CAST(AVG(pd.Score) AS decimal(5,2)) TeamAverage FROM dbo.TNTCriteria c CROSS JOIN dbo.TNTEvaluationPeriods p LEFT JOIN PD pd ON pd.CriterionId=c.CriterionId AND pd.PeriodId=p.PeriodId WHERE c.IsActive=1 AND p.IsActive=1 AND p.SchoolYear=@SchoolYear GROUP BY c.CriterionId,c.Name,c.SortOrder,c.Category,p.PeriodId,p.Name,p.SortOrder ORDER BY c.SortOrder,p.SortOrder`)).recordset;const dancers=(await pool.request().input('TeamId',sql.Int,teamId).input('SchoolYear',sql.NVarChar(20),schoolYear).query(`WITH PE AS(SELECT e.DancerId,e.PeriodId,AVG(CAST(s.Score AS decimal(6,3))) Overall FROM dbo.TNTEvaluations e JOIN dbo.TNTEvaluationScores s ON s.EvaluationId=e.EvaluationId JOIN dbo.TNTDancers d ON d.DancerId=e.DancerId JOIN dbo.TNTEvaluationPeriods p ON p.PeriodId=e.PeriodId WHERE d.TeamId=@TeamId AND p.SchoolYear=@SchoolYear GROUP BY e.DancerId,e.PeriodId) SELECT d.DancerId,d.FirstName,d.LastName,p.PeriodId,p.Name PeriodName,CAST(pe.Overall AS decimal(5,2)) OverallScore FROM dbo.TNTDancers d CROSS JOIN dbo.TNTEvaluationPeriods p LEFT JOIN PE pe ON pe.DancerId=d.DancerId AND pe.PeriodId=p.PeriodId WHERE d.TeamId=@TeamId AND d.IsActive=1 AND p.SchoolYear=@SchoolYear AND p.IsActive=1 ORDER BY d.FirstName,d.LastName,p.SortOrder`)).recordset;return{status:200,headers:corsHeaders(),jsonBody:{team,schoolYear,periods,parameters:params,dancers}}}catch(e){ctx.error(e);return{status:e.status||500,headers:corsHeaders(),jsonBody:{error:e.message||'Teamstatistieken fout.'}}}}});
