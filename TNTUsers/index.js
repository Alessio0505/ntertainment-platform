const {app}=require('@azure/functions');const {getPool,sql}=require('../shared/db');const {corsHeaders,requireTntUser}=require('../shared/auth');
app.http('TNTUsers',{methods:['GET','PUT','OPTIONS'],authLevel:'anonymous',handler:async(req,ctx)=>{if(req.method==='OPTIONS')return{status:204,headers:corsHeaders()};try{const admin=requireTntUser(req,['Admin']),pool=await getPool();if(req.method==='GET'){const r=await pool.request().query(`SELECT u.UserId,u.FirstName,u.LastName,u.Email,u.Role,ut.TeamId FROM dbo.Users u LEFT JOIN dbo.TNTUserTeams ut ON ut.UserId=u.UserId ORDER BY u.LastName,u.FirstName,u.Email,ut.TeamId`);const m=new Map();for(const x of r.recordset){if(!m.has(x.UserId))m.set(x.UserId,{UserId:x.UserId,FirstName:x.FirstName,LastName:x.LastName,Email:x.Email,Role:x.Role,TeamIds:[]});if(x.TeamId)m.get(x.UserId).TeamIds.push(x.TeamId)}return{status:200,headers:corsHeaders(),jsonBody:[...m.values()]}}const b=await req.json(),roles=['Parent','TNTEvaluator','Admin'],teamIds=[...new Set((b.teamIds||[]).map(Number).filter(Number.isInteger))];if(!b.userId||!roles.includes(b.role))return{status:400,headers:corsHeaders(),jsonBody:{error:'Ongeldige gebruiker of rol.'}};if(+b.userId===+admin.userId&&b.role!=='Admin')return{status:400,headers:corsHeaders(),jsonBody:{error:'Je kunt je eigen Admin-rol hier niet verwijderen.'}};if(b.role==='TNTEvaluator'&&!teamIds.length)return{status:400,headers:corsHeaders(),jsonBody:{error:'Kies minstens één team voor een TNT Evaluator.'}};const tx=new sql.Transaction(pool);await tx.begin();try{await new sql.Request(tx).input('UserId',sql.Int,+b.userId).input('Role',sql.NVarChar(30),b.role).query(`UPDATE dbo.Users SET Role=@Role WHERE UserId=@UserId; DELETE FROM dbo.TNTUserTeams WHERE UserId=@UserId;`);if(b.role==='TNTEvaluator')for(const teamId of teamIds)await new sql.Request(tx).input('UserId',sql.Int,+b.userId).input('TeamId',sql.Int,teamId).query(`INSERT INTO dbo.TNTUserTeams(UserId,TeamId) SELECT @UserId,@TeamId WHERE EXISTS(SELECT 1 FROM dbo.TNTTeams WHERE TeamId=@TeamId AND IsActive=1)`);await tx.commit();return{status:200,headers:corsHeaders(),jsonBody:{success:true}}}catch(e){await tx.rollback();throw e}}catch(e){ctx.error(e);return{status:e.status||500,headers:corsHeaders(),jsonBody:{error:e.message||'Gebruikersbeheer fout.'}}}}});
